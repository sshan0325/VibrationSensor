17.06.14

[출력]
- FFT결과 중, 크기가 큰 상위 (3개->5개)에 대한 크기 및 주파수 데이터 출력


[기타 추가사항]
- 진동 감지시 LED 표시(Green On)
- 데이터 송신시 LED 표시(Blue On)
- 데이터 송신 완료시 LED 초기화(Green/Blue : Off)

------------------------------------------------------------------------------

17.06.20

[출력]
(우선 Sensor Monitor 3을 사용할 것!)
- 6 바이트를 수신 받으면 LED를 깜빡이면서 5개의 패킷을 날림 (USART1 사용)

------------------------------------------------------------------------------

17.06.26

[출력]
- RS485 송/수신 가능
- PC 프로그램으로부터 6바이트를 받으면 10바이트 데이터 송신 (아직 ID구별 없음)

------------------------------------------------------------------------------

17.06.30
[출력]
- CMD를 받으면 내용에 따라 감지될 때마다 송신/요청받을때만 송신 모드로 나뉨 
  --> PC 프로그램: Sensor Monitor 11을 사용해야함

------------------------------------------------------------------------------

17.09.18
(F:\업무\FF. 현산 기술연구소\1. 층간소음_진동센서\Test_170630 에 있는 자료를 사용)

[출력]
- 3바이트 추가(상위 2~5위의 크기 평균값 정수, 소수 및 단계)
- 평균값을 dB로 환산하는 계산식(calc_state()) 추가
  --> 계산식 안에서 dB에 따른 단계 판별기능 구현

-------------------------------------------------------------------------------
 
17.09.20

[출력]
예측식 factor값 요청하는 CMD 추가함
--> 사용된 예측식의 factor(일차항 계수 및 상수항) 보내기
--> 예측식이 현장에서 마다 조금씩 바뀔 수 있으므로 수정사항을 확인할 수 있게 하기 위함.

[수정]
--> 반드시 PC프로그램에서 'Mode 1'버튼이나 'Mode 2' 버튼을 눌러야 TX 활성화 커맨드를 받아 센서 값을 보낼 수 있도록 바꾸었음
--> if(Rx_Compli_Flag == SET) 부분 비활성화 시킴

[유의사항]
-->PC 프로그램은 'Motion_Sensor_Monitor_11_3'와 세트로 사용할 것!

-------------------------------------------------------------------------------

17.10.17
[출력]
--> 현산 기술연구소 요청으로 20단계 이상의 크기에서는 max_state를 100으로 표시하게 함
      (이전에는 1~20단계 이하, 이상에 대해서는 max_state를 0으로 표시했음)

-------------------------------------------------------------------------------
